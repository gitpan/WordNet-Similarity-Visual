NAME
    INSTALL - How to install WordNet::Similarity::Visual

SYNOPSIS

     perl Makefile.PL

     make

     make test

     make install

DESCRIPTION

  Prerequisites
    You need to have WordNet::Similarity (version 0.14 or better),
    WordNet::QueryData (version 1.30 or better), and Gtk2
    (version 2-1.00 or beter) installed.

    WordNet::Similarity is available from
    L<http://search.cpan.org/dist/WordNet-Similarity/>

    WordNet::QueryData is available from
    L<http://search.cpan.org/dist/WordNet-QueryData/>

    Gtk2 is available from
    L<http://search.cpan.org/dist/Gtk2/>

  Installing

    The usual way to install the package is to run the following commands:

     perl Makefile.PL
     make
     make test
     make install
